观察者模式
普通情况下，无法在运行时动态的添加第三方，也就是说如果想要加入第三方，需要在weatherDate类进行添加带动，而这种类往往会牵一发而动全身 不符合开闭原则
最好的情况就是不在这种类改进，而在客户端进行改进，也就是客户端是可以随意新增代码的，
不利于维护，也不利于动态加入

解决方法就是在weatherDate里维护一个观察者列表，同时有注册观察者（addList）和删除观察者（remove）的方法，然后有notify观察者的方法
当weatherDate进行setDate时调用通知观察者的方法，也就是逐一遍历观察者，调用观察者的update方法，从而实现动态推送，当有另外的公司希望获得天七
数据，只需要写一个实体类，然后在客户端进行添加观察者即可